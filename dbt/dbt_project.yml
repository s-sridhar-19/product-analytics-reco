name: 'product_analytics'
version: '1.0.0'
profile: 'product_analytics'
config-version: 2

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  product_analytics:
    staging:
      +materialized: view
    intermediate:
      +materialized: table
    marts:
      +materialized: table
